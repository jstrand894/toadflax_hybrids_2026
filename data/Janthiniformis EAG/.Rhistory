fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.7, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.7, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.7, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.7, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.7, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
library(ambient)
fid_line <-
dt_janthinus %>%
mutate(EAD = EAD + rollmean(rnorm(n(), mean = 0, sd = 0.3),
k = 20, fill = NA, align = "center")) %>%
mutate(rownum = row_number())
fid_line %>%
ggplot() +
geom_line(aes(x = rownum, y = -1*EAD)) +
ylim(min_y = -1.4,
max_y = 1.4)
spike_data <- tribble(
~name,        ~start,  ~size,
"sixmethyl",    1775,   0.55,
"z3hex",        2009,   1.19,
"limonene",     2363,   0.591,
"tbocimene",    2497,   1.43,
"ocimene",      3168,   0.89,
"linalool",     4753,   1.38,
"caryophyllene",5262,   0.77) %>%
mutate(size = size + runif(n(), min = 0.4, max = 2.5))
spike_values <- c(0.183, 0.352, 0.588, 0.810, 0.865, 0.822, 0.678, 0.618)
combined_spike_table <- spike_data %>%
mutate(
spike_value = map2(size*.5, list(spike_values), ~ .y * .x),
rownum = map2(start, spike_value, ~ .x:(.x + length(.y) - 1)),
spike_table = pmap(list(rownum, spike_value, name), ~ tibble(rownum = ..1, spike_value = ..2, name = ..3))
) %>%
select(spike_table) %>%
unnest(cols = spike_table) %>%
rename("EAD" = "spike_value") %>%
dplyr::select(-3)
fid_line_spikes <-
fid_line %>%
dplyr::select(-FID) %>%
filter(!rownum %in% combined_spike_table$rownum) %>%
bind_rows(combined_spike_table %>%
mutate(EAD)) %>%
left_join(fid_line %>%
select(-EAD), by = "rownum")
ggplotly(
fid_line_spikes %>%
ggplot() +
geom_line(aes(x = rownum, y = EAD*-0.1)) +
geom_line(aes(x = rownum, y = FID)) +
ylim(min_y = -1.4,
max_y = 1.4) +
theme(axis.title.y = element_blank(),
axis.text.y = element_blank(),
axis.ticks.y = element_blank(),
axis.ticks.x = element_blank(),
axis.text.x = element_blank(),
panel.background = element_blank(),
plot.background = element_blank(),
panel.grid = element_blank())
)
# Export datasets to Excel files
library(writexl)
# Create a function to prepare data for export
prepare_for_export <- function(data, dataset_name) {
# Get the original FID data and merge with spikes
result <- data %>%
select(rownum, FID) %>%
mutate(time = rownum) %>%  # You can adjust time conversion if needed
select(time, FID) %>%
filter(!is.na(FID))
# Write to Excel
write_xlsx(result, paste0(dataset_name, ".xlsx"))
cat("Exported:", dataset_name, ".xlsx\n")
}
# Export each dataset
prepare_for_export(janthiniformis_dt_spikes %>%
left_join(janthiniformis_dt %>% select(rownum, FID), by = "rownum"),
"janthiniformis_dt_with_spikes")
library(tidyverse)
library(readxl)
library(baseline)
install.packages("baseline")
library(tidyverse)
library(readxl)
library(baseline)
library(plotly)
library(fuzzyjoin)
install.packages("fuzzyjoin")
library(tidyverse)
library(readxl)
library(baseline)
library(plotly)
library(fuzzyjoin)
library(ggforce)
install.packages("ggforce")
library(tidyverse)
library(readxl)
library(baseline)
library(plotly)
library(fuzzyjoin)
library(ggforce)
library(zoo)
library(grid)
library(cowplot)
# Export datasets to Excel files
library(writexl)
# Create a function to prepare data for export
prepare_for_export <- function(data, dataset_name) {
# Get the original FID data and merge with spikes
result <- data %>%
select(rownum, FID) %>%
mutate(time = rownum) %>%  # You can adjust time conversion if needed
select(time, FID) %>%
filter(!is.na(FID))
# Write to Excel
write_xlsx(result, paste0(dataset_name, ".xlsx"))
cat("Exported:", dataset_name, ".xlsx\n")
}
# Export each dataset
prepare_for_export(janthiniformis_dt_spikes %>%
left_join(janthiniformis_dt %>% select(rownum, FID), by = "rownum"),
"janthiniformis_dt_with_spikes")
prepare_for_export(dt_janthinus_spikes %>%
left_join(dt_janthinus %>% select(rownum, FID), by = "rownum"),
"dt_janthinus_with_spikes")
prepare_for_export(yt_janthiniformis_spikes %>%
left_join(yt_janthiniformis %>% select(rownum, FID), by = "rownum"),
"yt_janthiniformis_with_spikes")
prepare_for_export(yt_janthinus_spikes, "yt_janthinus_with_spikes")
prepare_for_export(d6y6_janthiniformis_spikes, "d6y6_janthiniformis_with_spikes")
prepare_for_export(d6y6_janthinus_spikes, "d6y6_janthinus_with_spikes")
prepare_for_export(y6d6_janthiniformis_spikes, "y6d6_janthiniformis_with_spikes")
prepare_for_export(fid_line_spikes, "baseline_with_spikes")
library(tidyverse)
library(readxl)
library(plotly)
library(cowplot)
# ── load_eag_fid() ──────────────────────────────────────────────────────────
# Reads one EAG file and one FID file, then inner-joins them on the shared
# time/rownum index. The inner join naturally trims the FID to the window
# already encoded in the EAG file — no manual row filtering needed.
#
# Arguments:
#   eag_path  : path to the pre-processed EAG xlsx (columns: time, FID*)
#   fid_path  : path to the FID xlsx (columns: EAD AVE, FID AVE, EAD 1, FID 1)
#
# * The EAG "FID" column is actually the EAD signal (instrument mislabeling).
#
# Returns a tibble with: time, EAD (depolarization), FID (flame ionization)
# ────────────────────────────────────────────────────────────────────────────
load_eag_fid <- function(eag_path, fid_path) {
eag <- read_excel(eag_path) %>%
rename(time = 1, EAD = 2)         # col 2 "FID" is EAD — see note 3 in change log
fid <- read_excel(fid_path) %>%
mutate(rownum = row_number()) %>%  # add sequential index since new files lack rownum
select(rownum, FID = `FID AVE`)    # only FID AVE needed; EAD comes from EAG file
eag %>%
inner_join(fid, by = c("time" = "rownum"))
}
# ── plot_eag_fid() ──────────────────────────────────────────────────────────
# Produces a clean dual-trace plot (FID on top, EAD inverted below).
# Strip all axis labels/ticks for a clean chromatogram look.
#
# Arguments:
#   data      : joined tibble from load_eag_fid()
#   ead_scale : multiplier applied to EAD before plotting (visual scaling only)
#   ead_flip  : if TRUE, EAD is plotted as negative (flips trace below baseline)
#   title     : optional plot title
# ────────────────────────────────────────────────────────────────────────────
plot_eag_fid <- function(data, ead_scale = 1, ead_flip = TRUE, title = NULL) {
ead_sign <- if (ead_flip) -1 else 1
p <- ggplot(data) +
geom_line(aes(x = time, y = FID), linewidth = 0.4) +
geom_line(aes(x = time, y = ead_sign * EAD * ead_scale),
color = "steelblue", linewidth = 0.4) +
theme(
axis.title   = element_blank(),
axis.text    = element_blank(),
axis.ticks   = element_blank(),
panel.background = element_blank(),
plot.background  = element_blank(),
panel.grid       = element_blank()
)
if (!is.null(title)) p <- p + ggtitle(title)
p
}
# EAG file : data/EAG/janthiniformis_dt_with_spikes.xlsx  (5,999 rows, time 2501–8499)
# FID file : data/FID/DT EAGFID 2.10.xlsx                 (13,479 rows)
#
# Original EAD scale: 0.25  (EAD * 0.25 applied before plotting in original code)
# Original EAD direction: inverted (negative)
dt_janthiniformis <- load_eag_fid(
eag_path = "../data/EAG/janthiniformis_dt_with_spikes.xlsx",
fid_path = "../data/FID/DT EAGFID 2.10.xlsx"
)
dt_janthinus <- load_eag_fid(
eag_path = "dt_janthinus_with_spikes.xlsx",
fid_path = "../data/FID/DT_Janthinus.xlsx"
)
dt_janthinus <- load_eag_fid(
eag_path = "dt_janthinus_with_spikes.xlsx",
fid_path = "../FID/DT_Janthinus.xlsx"
)
source("helpers_eag_fid.R")
getwd()
library(here)
source(here("helpers_eag_fid.R"))
install.packages("here")
library(here)
source(here("helpers_eag_fid.R"))
library(here)
here()
